spotless {
    java {
        googleJavaFormat()
        target('database-service-plugin/**/*.java', 'database-service-core/**/*.java')
        removeUnusedImports()
        endWithNewline()
        trimTrailingWhitespace()
        indentWithTabs(2)
    }
}
